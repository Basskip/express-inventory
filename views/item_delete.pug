extends layout

block content

  h1 Item: #{item.name}

  p Do you really want to delete this Item?

  form(method='POST' action='')
    div
      input#item-id(type='hidden', name='itemid', required='true', value=item._id)
    
    button(type='submit') Delete