extends layout

block content
  h1= item.name

  p Cost: #{item.cost} gold
  p Description: #{item.description}
  p Category: 
    a(href=item.category.url) #{item.category.name}

  a(href=item.url + '/delete') Delete
  a(href=item.url + '/update') Update